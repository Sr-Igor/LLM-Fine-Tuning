# ==========================================
# 🔐 AUTENTICAÇÃO E ACESSOS
# ==========================================

# Hugging Face Token (OBRIGATÓRIO)
# Necessário para baixar modelos "Gated" como Llama 3.1 ou fazer upload do seu modelo final.
# Crie um token com permissão "Write" em: https://huggingface.co/settings/tokens
HF_TOKEN=hf_seu_token_aqui_xyz123

# ==========================================
# 📊 MONITORAMENTO (Opcional, mas Recomendado)
# ==========================================

# Weights & Biases (WandB)
# Gera gráficos lindos de Loss, VRAM e progresso em tempo real no navegador.
# Crie conta em: https://wandb.ai/
WANDB_API_KEY=
WANDB_PROJECT=planus-llm
WANDB_WATCH=false  # false é mais leve, true loga gradientes (pesado)

# ==========================================
# 🤖 CONFIGURAÇÕES DO OLLAMA (Gerador de Dados)
# ==========================================

# Endereço do Ollama (Padrão: http://localhost:11434)
# Se estiver rodando em docker ou outra máquina, altere aqui.
OLLAMA_HOST=http://localhost:11434

# Modelo usado para ler seus PDFs e criar as perguntas/respostas.
# IMPORTANTE: Você deve ter baixado o modelo antes (ex: ollama pull qwen2.5)
#
# Opções Recomendadas:
# - llama3.1       (O Padrão. Equilíbrio perfeito entre velocidade e obediência a JSON)
# - qwen2.5        (Muitas vezes superior em lógica e multilíngue. Use 'qwen2.5:14b' se puder)
# - mistral-nemo   (Excelente janela de contexto e raciocínio)
# - deepseek-r1    (Ótimo raciocínio, mas pode ser lento e verboso para gerar JSON simples)
GENERATOR_MODEL=llama3.1

# ==========================================
# ⚙️ SISTEMA E CACHE (Avançado)
# ==========================================

# Caminho customizado para salvar os modelos baixados (Hugging Face Cache).
# Útil se seu disco principal (C: ou /) for pequeno e você tiver um HD secundário.
# Se deixar comentado, usa o padrão do sistema (~/.cache/huggingface).
# HF_HOME=/mnt/dados/huggingface_cache

# Define qual GPU usar (Se tiver múltiplas, ex: 0,1). Deixe vazio para usar todas.
CUDA_VISIBLE_DEVICES=0